{
  "name": "phonebook",
  "version": "0.0.1",
  "description": "part 3 exercises - phonebook backend",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf dist && cd ../submissions_fso/part2/phonebook && npm run build && cp -r dist ../../../submissions_fso_part3",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",
    "logs:prod": "fly logs",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "github.com/bistik/submissions_fso_part3"
  },
  "author": "Erick B",
  "license": "MIT",
  "devDependencies": {
    "@flydotio/dockerfile": "^0.7.8",
    "nodemon": "^3.1.9"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.10.1",
    "morgan": "^1.10.0"
  }
}
